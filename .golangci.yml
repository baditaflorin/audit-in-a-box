version: "2"
linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - revive
    - gosec
    - gocyclo
    - misspell
    - unused
    - ineffassign
  settings:
    revive:
      rules:
        - name: package-comments
          disabled: true
        - name: exported
          disabled: true
        - name: increment-decrement
          disabled: true
run:
  timeout: 3m
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
